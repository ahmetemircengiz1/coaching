import Link from "next/link";
import { Mail, Phone, MapPin } from "lucide-react";

const LEGAL_LINKS = [
  { slug: "gizlilik", label: "Gizlilik Politikası" },
  { slug: "kvkk", label: "KVKK Aydınlatma Metni" },
  { slug: "kullanim", label: "Kullanım Koşulları" },
  { slug: "mesafeli", label: "Mesafeli Satış Sözleşmesi" },
  { slug: "iade", label: "İade ve İptal Politikası" },
  { slug: "cerez", label: "Çerez Politikası" },
];

const QUICK_LINKS = [
  { href: "/platform", label: "Anasayfa" },
  { href: "/platform/pricing", label: "Paketler" },
  { href: "/platform/auth", label: "Koç Girişi" },
  { href: "/platform/onboarding", label: "Hemen Başla" },
];

export function PlatformFooter() {
  const supportEmail = process.env.NEXT_PUBLIC_SUPPORT_EMAIL || "destek@coachsite.com";
  const supportPhone = process.env.NEXT_PUBLIC_SUPPORT_PHONE || "";
  const businessAddress = process.env.NEXT_PUBLIC_BUSINESS_ADDRESS || "";

  return (
    <footer className="border-t border-white/5 bg-[#0a0a0d] text-white/70">
      <div className="container mx-auto max-w-6xl px-6 py-12">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-10">
          {/* Logo + Açıklama */}
          <div className="space-y-4">
            <Link href="/platform" className="font-heading text-2xl font-bold tracking-widest flex items-center gap-2 text-white">
              COACH<span className="text-[#ccff00]">OS</span>
            </Link>
            <p className="text-sm leading-relaxed text-white/50">
              Profesyonel koçlar için yeni nesil dijital platform. Markanızı dakikalar içinde
              ayağa kaldırın, danışanlarınıza özel deneyim sunun.
            </p>
          </div>

          {/* Hızlı Erişim */}
          <div>
            <h4 className="text-white font-semibold text-sm uppercase tracking-wider mb-4">
              Hızlı Erişim
            </h4>
            <ul className="space-y-2 text-sm">
              {QUICK_LINKS.map((link) => (
                <li key={link.href}>
                  <Link href={link.href} className="text-white/60 hover:text-white transition-colors">
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Yasal */}
          <div>
            <h4 className="text-white font-semibold text-sm uppercase tracking-wider mb-4">
              Yasal
            </h4>
            <ul className="space-y-2 text-sm">
              {LEGAL_LINKS.map((link) => (
                <li key={link.slug}>
                  <Link
                    href={`/platform/legal/${link.slug}`}
                    className="text-white/60 hover:text-white transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* İletişim */}
          <div>
            <h4 className="text-white font-semibold text-sm uppercase tracking-wider mb-4">
              İletişim
            </h4>
            <ul className="space-y-3 text-sm">
              {supportEmail && (
                <li className="flex items-start gap-2 text-white/60">
                  <Mail className="h-4 w-4 mt-0.5 shrink-0 text-[#ccff00]" />
                  <a href={`mailto:${supportEmail}`} className="hover:text-white transition-colors break-all">
                    {supportEmail}
                  </a>
                </li>
              )}
              {supportPhone && (
                <li className="flex items-start gap-2 text-white/60">
                  <Phone className="h-4 w-4 mt-0.5 shrink-0 text-[#ccff00]" />
                  <a href={`tel:${supportPhone.replace(/\s/g, "")}`} className="hover:text-white transition-colors">
                    {supportPhone}
                  </a>
                </li>
              )}
              {businessAddress && (
                <li className="flex items-start gap-2 text-white/60">
                  <MapPin className="h-4 w-4 mt-0.5 shrink-0 text-[#ccff00]" />
                  <span>{businessAddress}</span>
                </li>
              )}
            </ul>
          </div>
        </div>

        <div className="mt-12 pt-8 border-t border-white/5 flex flex-col sm:flex-row justify-between items-center gap-3 text-xs text-white/40">
          <p>&copy; {new Date().getFullYear()} Coach OS. Tüm hakları saklıdır.</p>
          <p>Made with <span className="text-[#ccff00]">●</span> Türkiye</p>
        </div>
      </div>
    </footer>
  );
}
